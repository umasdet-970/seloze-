{
  "name": "seloze-rules-test",
  "private": true,
  "description": "Tests for firestore.rules against the local Firestore emulator. Run: npm test",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-seloze \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1"
  }
}
