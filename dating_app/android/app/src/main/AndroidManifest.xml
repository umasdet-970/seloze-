<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Only in the debug manifest by default (added there for the Flutter
         tool's hot reload connection) — release builds need it declared
         here explicitly, or network image loading and any real backend
         calls silently fail once signed/released. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Ads are off, so the advertising-ID permissions that the ads/analytics
         plugins merge in are removed: Play would otherwise require an
         Advertising ID declaration and matching Data safety answers.
         If ads are switched on later (kUseAds), delete these four lines. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION" tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_TOPICS" tools:node="remove"/>
    <!-- Needed for image_picker's camera-capture option (Add photos step).
         Gallery access needs no declared permission on modern Android —
         image_picker uses the system Photos Picker, which doesn't require
         a runtime grant. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Android 13+ requires this to show any notification, including
         FCM push. firebase_messaging's own manifest merge usually adds
         this, but declaring it explicitly avoids depending on that. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Real geo-distance in Discover (spec section 4/15) — geolocator's
         own manifest merge would add these automatically, but declared
         explicitly here for the same reason as every other permission in
         this file: don't depend on a plugin's manifest merge behavior.
         Coarse is enough for "how many km away" — fine isn't requested,
         since this app has no use for street-level precision. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Chat voice notes (record package) — requested at the moment the
         user first taps the mic button, not on app launch. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- In-app voice/video calls (agora_rtc_engine — kUseVoiceVideoCalls,
         off until a real Agora account exists, see call_config.dart).
         CAMERA/RECORD_AUDIO are already declared above; these are Agora's
         remaining requirements for call routing and Bluetooth headsets. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <application
        android:label="Seloze"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Real Seloze AdMob app ID (admob.google.com, app "Seloze-Global
             Dating App"). Required for the SDK to initialize even while
             kUseAds is false / test ad units are still in use elsewhere —
             see lib/core/config/ad_config.dart. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4841811327884657~2139845035" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
